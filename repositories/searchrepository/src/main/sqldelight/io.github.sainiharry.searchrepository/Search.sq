CREATE TABLE SearchModel(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    searchQuery TEXT NOT NULL
);

selectSearchQueries:
SELECT * FROM SearchModel ORDER BY id LIMIT 5;

insert:
INSERT INTO SearchModel(searchQuery)
VALUES(?);